site/

# Added by ggshield
.cache_ggshield

# Local Claude Code agent state (scheduler locks, etc.) — root only,
# so scaffolded sub-projects (calculus-agent/.claude/commands/, etc.) commit cleanly.
/.claude/
