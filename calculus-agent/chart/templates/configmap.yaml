apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ecosystem-test-agent.fullname" . }}-config
  labels:
    {{- include "ecosystem-test-agent.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
