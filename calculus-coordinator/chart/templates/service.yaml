apiVersion: v1
kind: Service
metadata:
  name: {{ include "calculus-coordinator.fullname" . }}
  labels:
    {{- include "calculus-coordinator.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calculus-coordinator.selectorLabels" . | nindent 4 }}
